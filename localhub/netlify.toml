[build]
  base = "localhub"
  # npm run build를 실행하기 전에 node_modules/.bin/vite에 실행 권한을 강제로 부여합니다.
  command = "chmod +x ./node_modules/.bin/vite && npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200